// const els = document.getElementsByClassName('c');

// for (let i = 0; i < els.length; i++) {
//   els[i].style.backgroundColor = 'red';
// }

// const forEachWala = (element, index, array) => {
//   console.log(element, index, array);
// };

// arr.forEach(forEachWala);

// const clone = arr.map((element, index, array) => {
//   return element;
// });

// const arr = [1, 2, 3, 4, 5];

// const fun = (accumulator, currentValue, currentIndex, array) => {
//   accumulator = accumulator + currentValue;
//   return accumulator;
// };

// const total = arr.reduce(fun, 0);

// //  is john is doe is jane is doe

// const fun = (accumulator, currentValue, currentIndex, array) => {
//   accumulator.push(currentValue);
//   return accumulator;
// };

// const finalStr = arr.reduce(fun, []);

// console.log(finalStr);

const arr = [10, 5, 65, 23, 7, 345, 3454778, 23, 5, 6, 7, 8, 9, 10];

const str = arr.sort();

console.log(str);
